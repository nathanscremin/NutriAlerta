"""
Pacote src — módulos do pipeline ETL SISVAN Estado Nutricional.

Módulos:
    extractor      — extração via API com paginação
    cleaner        — limpeza e validação dos dados
    converter      — persistência em CSV e Parquet
    state_manager  — watermark e controle de execução
    paths          — caminhos padrão do projeto
"""
